<%= simple_form_for(@speaker) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :date %>
    <%= f.association :member, collection:Member.order('name ASC') %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
