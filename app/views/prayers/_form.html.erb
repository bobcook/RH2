<%= simple_form_for(@prayer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :date %>
    <%= f.association :member %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
